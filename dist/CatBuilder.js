"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class CatBuilder {
    constructor(objcomparer) {
        this.comparer = objcomparer;
    }
    createSets(objects, arrows) {
        // check validity
        // return
    }
    createGraphs(objects, arrows) {
        // check validity
        // return
    }
    createGraphsFromObject(object) {
        // check validity
        // return
    }
    typeToInstance(atype) {
        return (new atype());
    }
}
exports.CatBuilder = CatBuilder;
